"""
Test Bench for GRADIEND

This module contains integration tests that run actual training (not mocked).
These tests verify that training completes successfully and produces expected metrics.

Note: These tests take longer to run and require GPU resources.
They are designed to catch regressions in training behavior and performance.
"""
